# Local Python environment and caches
.venv/
__pycache__/
*.pyc

# Training artifacts
checkpoints/*.pt
checkpoints/*.tmp
logs/
training_summary.md

# Engine credentials — never commit the token (see README)
engine.env
