*.log
.DS_Store
.env

# Admin SSH keys — the private key must never be committed; the public key is inlined into
# docker-compose.yml (ADMIN_PUBLIC_KEYS), so neither file needs to be tracked.
hex-admin
hex-admin.pub
